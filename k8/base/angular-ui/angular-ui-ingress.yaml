apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: angular-ui-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: fscc.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: angular-ui
                port:
                  number: 80
